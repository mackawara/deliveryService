# The dashboard uses Yarn with the node-modules linker so that Vite, Vitest and
# Playwright resolve dependencies exactly as they do in CI and in production builds.
nodeLinker: node-modules
enableGlobalCache: true
compressionLevel: mixed
